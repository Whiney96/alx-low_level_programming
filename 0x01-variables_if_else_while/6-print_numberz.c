#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 for success
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
		putchar(digit);
	putchar('\n');
	return (0);
}
